extends layout

block content
 .row
    .col-lg-4.col-md-4.col-sm-4.col-xs-12
    .col-lg-4.col-md-4.col-sm-4.col-xs-12
        form.form-signin(action="/login",method="post",style="margin-top:100px;")
            h2.text-center Ingrese sus datos
            .form-group
                input.form-control(type="text",name="email",placeholder="Email")
            .form-group
                input.form-control(type="password",name="password",placeholder="Password")
            .text-center
                button.btn.btn-primary(type="submit") Iniciar sesión
            if !error==0
                .alert.alert-danger.alert-dismissible(style="margin-top:30px;")
                    p=error