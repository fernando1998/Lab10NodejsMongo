extends layout

block content
    .container
        .main.container
            .col-md-6.col-md-offset-3
                h1.display-4.m-b-2(align="center") DETALLE DE PRODUCTO
                .form-group
                    label(for="nombre") Nombre:
                    input#nombre.form-control(type="text", name="nombre", value=data.nombre, readonly)
                .form-group
                    label(for="descripcion") Descripción:
                    input#descripcion.form-control(type="text",name="descripcion",value=data.descripcion, readonly)
                .form-group 
                    label(for="precio") Precio:
                    input#precio.form-control(type="text",name="precio", value=data.precio, readonly)
                .text-center
                    a.btn.btn-warning(href="/producto") Volver